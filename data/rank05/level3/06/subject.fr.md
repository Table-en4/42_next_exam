Écris une fonction qui cherche toutes les occurrences d'un mot cible dans une grille 2D dans les 8 directions.

- Renvoie une liste de tuples `(x, y, code_direction)`.
- Renvoie `[]` si la grille ou le mot est vide.