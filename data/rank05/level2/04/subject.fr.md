Écris une fonction qui détermine le nombre minimal de salles de réunion nécessaires et attribue les créneaux sans chevauchement.

- Renvoie un tuple `(num_rooms, rooms)`.
- Renvoie `(0, [])` si la liste est vide.